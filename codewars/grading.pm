sub gradingStudents {
    @_ = @{$_[0]};
    @_ = map {
        $_ < 38 ? $_
        : ($_%5)==0 ? $_
        : (($_+1)%5)==0 ? $_+1
        : (($_+2)%5)==0 ? $_+2
        : $_
    }@_;
    return @_;
}
